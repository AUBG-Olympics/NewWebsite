[build]
base    = "frontend"
command = "pnpm install --no-frozen-lockfile && pnpm build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"
PNPM_FLAGS   = "--no-frozen-lockfile"

[[redirects]]
from = "/*"
to   = "/index.html"
status = 200
